<div>
  {{#each posts}}
    <div>
      <div>
        <h2>
          <a href="/post/{{id}}"> {{title}}</a>
        </h2>
        <p> Posted by {{User.username}} on {{format_date createdAt}}</p>
      </div>
      <div>
        {{body}}
      </div>
    </div>

  {{/each}}
</div>